"use strict";
let userNames = ['admin', 'zubair', 'hasnain', 'waqas', 'umer'];
if (userNames.length == 0) {
    console.log('We need to find some user;');
}
else {
    userNames = [];
    console.log('All user names have been remove. ' + userNames.length);
}
